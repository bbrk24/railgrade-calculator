{
  "name": "railgrade-calculator",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "parcel": "^2.7.0",
    "typescript": "^4.8.4"
  },
  "engines": {
    "node": ">=12"
  },
  "scripts": {
    "build": "parcel build src/index.html --no-source-maps",
    "start": "parcel serve src/index.html --open",
    "clean": "rm -R dist/*",
    "deploy": "gh-pages -d dist"
  }
}
